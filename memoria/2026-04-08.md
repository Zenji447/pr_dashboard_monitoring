# 2026-04-08

- El usuario pidió que el asistente se llame Kamus.
- El usuario quiere que Kamus le ayude a diario con trabajo DevOps, revisando y aprobando pull requests.
- Flujo de revisión aprendido: el título del PR debe incluir el número del work item.
- Para PRs hacia `develop`, además se valida release `r6.1` y sprint `sp69` en la rama fuente.
- Para PRs hacia `develop-pr`, la rama es más flexible por ser bugfix, pero el work item en el título sigue siendo obligatorio.
- Revisión de `dataPack`: si el manifest no viene en el commit, el componente debe existir en el manifest del ambiente/rama destino correspondiente; si el manifest sí viene en el commit, el nombre debe coincidir exactamente con el componente real, incluyendo case sensitive. Puede darse un commit mixto donde un datapack sea nuevo y otro no: el nuevo se valida contra el manifest incluido en ese mismo commit y el viejo se certifica contra la rama destino.
- Si es el primer datapack de ese tipo en el release, también hay que validar su tipo en `manifest-pipeline/release-06.1/deploy-secuence-r6.1.json` y `manifest/deploysequence.json`.
- Revisión de `force-app`: si el archivo es nuevo y el PR también agrega `package-metadata.xml`, se valida en esa misma rama/commit; si `package-metadata.xml` no viene en el commit, se busca en la rama destino correspondiente. Si no está o está mal escrito, no está correcto para aprobar. La misma lógica aplica conceptualmente a datapacks.
- Si todas las validaciones salen bien, el PR se aprueba; si fallan, no avanza.
- Slack es parte del flujo operativo: después de aprobar o rechazar un PR, hay que comunicarlo por Slack.
- El usuario prefiere que la integración de Slack opere con contexto de usuario y no como bot visible `Kamus`.
- Se creó una app de Slack llamada `Kamus` en el workspace `Nespon PS`.
- Ya se obtuvo el `appToken` y el `signingSecret` de la app de Slack.
- Quedó pendiente completar OAuth para obtener especialmente el `User OAuth Token` (`xoxp-...`) y terminar la integración nativa de Slack en OpenClaw.
- El canal de Slack del equipo DevOps del usuario tiene id `C07M7GA60AF`.
- El usuario de Slack Angello tiene id `U0608RXSDUY`.
- Júlio, líder del usuario, tiene el chat/identificador de Slack `D09C90ZFMLM`.
- Verne Vivas tiene id de Slack `U09BT7CHL7M`.
- El canal diario donde llegan los pull requests es `C080K9D6EG2` (`proj-claroco-devops-requests`).
- Ese canal diario de requests está relacionado directamente con la hoja de Google Sheets `B2C Pull Request to Review - DevOps Team` (`https://docs.google.com/spreadsheets/d/1Sm_yOJvHMaPMvT1tqTH2NtYVhg62_6RERrTVvfcxACY/edit?pli=1&gid=428103390#gid=428103390`).
- Matias tiene el chat/identificador de Slack `D0AP824CA9J`.
- Gustavo Alonso Muciño (TA Reviewer) tiene el chat/identificador de Slack `D09JSKX0JC8`.
